{
  "name": "anha",
  "version": "1.0.0",
  "description": "Arabic-first maintenance booking app for ANHA Trading (شركة انها التجارية). Uses SQLite for development and PostgreSQL for production.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @tailwindcss/cli -i app/static/css/input.css -o app/static/css/main.css --minify",
    "watch": "npx @tailwindcss/cli -i app/static/css/input.css -o app/static/css/main.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
